namespace Domain;

// Marker type used to reference the Domain assembly from other projects
// Keep this class empty; it's only used for Assembly scanning at runtime
public static class AssemblyReference
{
}
